import { AddHolder } from "./AddHome/addHolder"
import ImageSlider from "./imageSlider"

export const HomePage = ()=>{
    return(
        <main>
            <ImageSlider/>
            <AddHolder/>
        </main>
    )
}